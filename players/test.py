# This file will be used in the competition
# Please make sure the following functions are well defined
'''
from advance_model import *
from utils import *
import math
class myPlayer(AdvancePlayer):

    # initialize
    # The following function should not be changed at all
    def __init__(self,_id):
        super().__init__(_id)

    # Each player is given 5 seconds when a new round started
    # If exceeds 5 seconds, all your code will be terminated and
    # you will receive a timeout warning
    def StartRound(self,game_state):
        return None

    # Each player is given 1 second to select next best move
    # If exceeds 5 seconds, all your code will be terminated,
    # a random action will be selected, and you will receive
    # a timeout warning
    def SelectMove(self, moves, game_state):
        if moves==3:
           myscore = 1
           score2 =








   """ def function(deep, alpha, beta, role, step, steps, spread):
     #   score = None
        MAX =
        MIN =
        score  = evaluate(role)

    def minimax(game_state, depth , self):
        if self == max:
            return [-1,-1,-infinity]
        else:
            return [-1,-1,+infinity]

    def leaf:
        score = _e
        step = step
        steps = steps
        return leaf

    def best :
        score = MIN
        step = step
        steps = steps


    points = board.gen(role)

    if (!points.length) return leaf

    for  i=0;i < points.length;i++ :
        p = points[i]
    board.put(p, role)

     deep = deep-1

     spread = spread

     steps = steps.slice(0)
    _steps.push(p)
     v = r(_deep, -beta, -alpha, R.reverse(role), step+1, _steps, _spread)
    v.score *= -1
    board.remove(p)
    return best

"""
'''

